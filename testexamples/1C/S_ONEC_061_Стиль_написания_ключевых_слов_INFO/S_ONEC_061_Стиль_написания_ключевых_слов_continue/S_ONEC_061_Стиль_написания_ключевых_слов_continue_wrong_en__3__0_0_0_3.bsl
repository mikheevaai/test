For Each Element In Collection Do
    If Element = 5 Then
        // S_ONEC_061 Стиль написания ключевых слов x1
        continue;
    EndIf;
EndDo;

For Each Element In Collection Do
    If Element = 5 Then
        // S_ONEC_061 Стиль написания ключевых слов x1
        cONtiNuE;
    EndIf;
EndDo;

For Each Element In Collection Do
    If Element = 5 Then
        // S_ONEC_061 Стиль написания ключевых слов x1
        CONTINUE;
    EndIf;
EndDo;
