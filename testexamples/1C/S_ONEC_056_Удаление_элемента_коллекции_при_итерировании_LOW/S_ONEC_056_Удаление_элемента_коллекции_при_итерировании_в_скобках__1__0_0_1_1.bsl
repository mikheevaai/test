Функция ПримерФункции(Коллекция)
    Для Каждого Число Из Коллекция Цикл
        // S_ONEC_059 Незакрытый оператор x1
        Если Число % 2 <> 0 Тогда
            // S_ONEC_056 Удаление элемента коллекции при итерировании x1
            Коллекция.Удалить(Число);
        КонецЕсли
    КонецЦикла;
КонецФункции
