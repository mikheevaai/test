For Each Element In Collection Do
    If Element = 5 Then
        // S_ONEC_061 Стиль написания ключевых слов x1
        break;
    EndIf;
EndDo;

For Each Element In Collection Do
    If Element = 5 Then
        // S_ONEC_061 Стиль написания ключевых слов x1
        bReAk;
    EndIf;
EndDo;

For Each Element In Collection Do
    If Element = 5 Then
        // S_ONEC_061 Стиль написания ключевых слов x1
        BREAK;
    EndIf;
EndDo;
