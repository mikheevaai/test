// S_ONEC_061 Стиль написания ключевых слов x1
Для каждого Элемент Из Коллекция Цикл
    Обработать(Элемент);
КонецЦикла;

// S_ONEC_061 Стиль написания ключевых слов x1
Для каЖДоГо Элемент Из Коллекция Цикл
    Обработать(Элемент);
КонецЦикла;

// S_ONEC_061 Стиль написания ключевых слов x1
Для КАЖДОГО Элемент Из Коллекция Цикл
    Обработать(Элемент);
КонецЦикла;
